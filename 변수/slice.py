# slice := 연속적 객체들의 범위를 지정해 선택된 객체만 가져오는 방법
# slicing -> 새로운 객체 생성 = 새로운 메모리 할당

#[1] 문자열
t='korea'

# [2] 슬라이스 및 id() 출력
print(t, id(t), '-', t[:1], id(t[:1]), '-', t[:2], id(t[:2]), '-', t[:3], id(t[:3]), '-', t[:4], id(t[:4])    #---다 다름--;;
print(t[:], id(t[:])
      
# [3]
t+='!'
print(t, id(t))
a=t[:-1]
print(a, id(a))
